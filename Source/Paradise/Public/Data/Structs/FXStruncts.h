#pragma once

#include "CoreMinimal.h"
#include "NiagaraSystem.h"       // 나이아가라 사용 위함
#include "Sound/SoundBase.h"     // 사운드 사용 위함
#include "FXStructs.generated.h"

/**
 * @struct FCombatFXSet
 * @brief 시각(VFX)과 청각(SFX) 효과를 하나로 묶은 패키지
 */
USTRUCT(BlueprintType)
struct FCombatFXSet
{
	GENERATED_BODY()
};